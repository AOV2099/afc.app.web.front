# The gateway binds to loopback so only local ngrok/Cloudflare agents can reach it.
GATEWAY_HOST=127.0.0.1
GATEWAY_PORT=3010

# Host-published AFC services
AFC_FRONT_TARGET=http://127.0.0.1:3005
AFC_BACK_TARGET=http://127.0.0.1:3004
